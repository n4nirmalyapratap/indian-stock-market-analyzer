[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "pandas_ta"
version = "0.3.14b0"
description = "Local shim — wraps the ta library with pandas_ta-compatible API"
requires-python = ">=3.10"
dependencies = ["pandas", "ta"]
