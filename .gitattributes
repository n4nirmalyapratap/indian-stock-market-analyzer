artifacts/python-backend/data/nse_options_cache.sqlite filter=lfs diff=lfs merge=lfs -text

# Force LF line endings for shell scripts on all platforms (prevents CRLF
# corruption on Windows CI runners which breaks #!/bin/sh shebangs in Docker)
*.sh        text eol=lf
Dockerfile* text eol=lf
